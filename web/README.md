Frontend del MVP (Vite + React + TypeScript)

Instalación y ejecución:

```bash
cd web
npm install
npm run dev
```

Abrir `http://localhost:5173` y probar el componente "ColorFilterViewer".

Nota: el ejemplo carga por defecto `/Assets/baseball_cap.glb` si existe en el repositorio.
